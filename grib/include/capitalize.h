#pragma once
#include <stdio.h>
#include "def.h"

extern int capitalize(const char* in,
                        const char* root_cap_dir_name,
                        const char* fmt_cap,
                        enum DATA_FORMAT output_type);