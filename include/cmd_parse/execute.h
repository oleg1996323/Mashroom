#pragma once
#include <vector>
#include <string>
#include <string_view>
#include <ranges>

    #include "capitalize.h"
    #include "decode.h"

void execute(const std::vector<std::string_view>& argv);